const ACTIONS = {
    SetLoggedInUser : "SetLoggedInUser"
}

const MUTATIONS = {
    SetLoggedInUser : "SetLoggedInUser"
}

const GETTERS = {
    LoggedInUser : "LoggedInUser"
}

export {
    ACTIONS,
    MUTATIONS,
    GETTERS
}